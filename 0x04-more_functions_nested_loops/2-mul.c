#include "main.h"

/**
 * mul - multiplies two integers and outputs the result
 *
 * @a: the first input integer
 *
 * @b: the second input integer
 *
 * Return: result
*/
int mul(int a, int b)
{
	int result = (a * b);

	return (result);
}
