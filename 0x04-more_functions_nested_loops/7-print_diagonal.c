#include "main.h"

/**
 * print_diagonal - prints a diagonal
 *
 * @n: integer accepted
 *
 * Return: 0
*/
void print_diagonal(int n)
{
	int i = 0;

	while (i <= n && n >= 0)
	{
		if (n = 0)
			_putchar('\n');
		else
			_putchar('\'');
	}
	i++;
	_putchar('\n');
}
