#include "main.h"

/**
 * _isdigit - checks if an integer is a digit or not
 *
 * @c: the integer it accepts
 *
 * Return: 1 if True, 0 if false
*/
int _isdigit(int c)
{
	return (c >= 0 && c <= 9);
}
